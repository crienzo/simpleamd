simpleamd : Simple Answering Machine Detector

An answering machine detection library that uses a simple energy-based voice activity detector.
This detector accepts 16-bit signed little-endian PCM samples.

To build:

./autogen.sh
./configure
make
make install


See simpleamd.c for an example of use.

